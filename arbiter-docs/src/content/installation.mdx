# Installation

Install the Arbiter SDK:

```bash
npm install arbiter-sdk
```

Example usage:

```javascript
import { Arbiter } from "arbiter-sdk"

const arbiter = new Arbiter({
  apiKey: "your-api-key"
})
```